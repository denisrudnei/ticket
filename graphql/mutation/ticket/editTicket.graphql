#import "@/graphql/fragments/ticketList.graphql"
mutation EditTicket($id: ID!, $ticket: TicketCreateInput!) {
  ticket: EditTicket(id: $id, ticket: $ticket) {
    id
    ...ticketList
    content
  }
}