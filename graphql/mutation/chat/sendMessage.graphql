mutation SendMessage($to: ID!, $message: String!){
  message: SendMessage(to: $to, message: $message) {
    date
    content
    to {
      name
      _id
      picture
    }
    from {
      name
      _id
      picture
    }
  }
}