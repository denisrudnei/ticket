mutation MergeUser($email: String!, $user: AnalystInput!) {
  MergeUser(email: $email, user: $user) {
    id
    name
    email
    contactEmail
    color
    role {
      id
      name
    }
    picture
    address {
      id
    }
  }
}
