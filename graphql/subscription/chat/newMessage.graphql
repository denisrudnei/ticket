#import "@/graphql/fragments/participantInfo.graphql"
subscription NewMessage($to: ID!) {
  message: NewMessage(to: $to) {
    id
    content
    date
    to {
      ...participantInfo
    }
    from {
      ...participantInfo
    }
  }
}