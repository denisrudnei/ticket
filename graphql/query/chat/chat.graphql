#import "@/graphql/fragments/participantInfo.graphql"
query {
  chat: Chat {
    id
    participants {
      ...participantInfo
    }
    messages {
      id
      to {
        id
        name
        picture
      }
      from {
        id
        name
        picture
      }
      date
      content
    }
  }
}