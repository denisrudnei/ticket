query KnowledgeById ($id: ID!) {
  knowledge: KnowledgeById(id: $id) {
    id
    name
    description
    category {
      id
      name
    }
    status {
      id
      name
    }
    group {
      id
      name
    }
  }
}