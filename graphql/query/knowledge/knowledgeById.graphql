#import "@/graphql/fragments/knowledge.graphql"
query KnowledgeById($id: ID!){
  knowledge: KnowledgeById(id: $id) {
    ...list
    group {
      id
      name
    }
    category {
      id
      fullName
      name
    }
    preview
    url
  }
}